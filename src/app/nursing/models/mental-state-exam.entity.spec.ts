import { MentalStateExamEntity } from './mental-state-exam.entity';

describe('MentalStateExamEntity', () => {
  it('should create an instance', () => {
    expect(new MentalStateExamEntity()).toBeTruthy();
  });
});
